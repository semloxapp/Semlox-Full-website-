-- WARNING: This schema is for context only and is not meant to be run.
-- Table order and constraints may not be valid for execution.

CREATE TABLE public.audit_log (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  company_id uuid,
  user_id uuid,
  action USER-DEFINED NOT NULL,
  resource_type text,
  resource_id uuid,
  description text,
  old_values jsonb,
  new_values jsonb,
  ip_address inet,
  user_agent text,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT audit_log_pkey PRIMARY KEY (id),
  CONSTRAINT audit_log_company_id_fkey FOREIGN KEY (company_id) REFERENCES public.companies(id),
  CONSTRAINT audit_log_user_id_fkey FOREIGN KEY (user_id) REFERENCES auth.users(id)
);
CREATE TABLE public.awb_history (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  awb_id uuid NOT NULL,
  company_id uuid NOT NULL,
  versioned_by uuid NOT NULL,
  payload jsonb NOT NULL,
  change_summary text,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT awb_history_pkey PRIMARY KEY (id),
  CONSTRAINT awb_history_awb_id_fkey FOREIGN KEY (awb_id) REFERENCES public.awbs(id),
  CONSTRAINT awb_history_company_id_fkey FOREIGN KEY (company_id) REFERENCES public.companies(id),
  CONSTRAINT awb_history_versioned_by_fkey FOREIGN KEY (versioned_by) REFERENCES auth.users(id)
);
CREATE TABLE public.awb_numbers (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  company_id uuid NOT NULL,
  airline_prefix text,
  awb_number text NOT NULL UNIQUE,
  status USER-DEFINED NOT NULL DEFAULT 'available'::awb_number_status,
  added_by uuid,
  added_at timestamp with time zone NOT NULL DEFAULT now(),
  consumed_at timestamp with time zone,
  consumed_by uuid,
  metadata jsonb DEFAULT '{}'::jsonb,
  CONSTRAINT awb_numbers_pkey PRIMARY KEY (id),
  CONSTRAINT awb_numbers_company_id_fkey FOREIGN KEY (company_id) REFERENCES public.companies(id),
  CONSTRAINT awb_numbers_added_by_fkey FOREIGN KEY (added_by) REFERENCES auth.users(id),
  CONSTRAINT awb_numbers_consumed_by_fkey FOREIGN KEY (consumed_by) REFERENCES auth.users(id)
);
CREATE TABLE public.awbs (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  company_id uuid NOT NULL,
  awb_number text UNIQUE,
  issued_by uuid,
  issued_at timestamp with time zone,
  created_by uuid NOT NULL,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_by uuid,
  status USER-DEFINED NOT NULL DEFAULT 'draft'::awb_status,
  payload jsonb,
  source_document_id uuid,
  metadata jsonb DEFAULT '{}'::jsonb,
  CONSTRAINT awbs_pkey PRIMARY KEY (id),
  CONSTRAINT awbs_company_id_fkey FOREIGN KEY (company_id) REFERENCES public.companies(id),
  CONSTRAINT awbs_issued_by_fkey FOREIGN KEY (issued_by) REFERENCES auth.users(id),
  CONSTRAINT awbs_created_by_fkey FOREIGN KEY (created_by) REFERENCES auth.users(id),
  CONSTRAINT awbs_updated_by_fkey FOREIGN KEY (updated_by) REFERENCES auth.users(id),
  CONSTRAINT awbs_source_document_id_fkey FOREIGN KEY (source_document_id) REFERENCES public.documents(id)
);
CREATE TABLE public.batch_jobs (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  company_id uuid NOT NULL,
  created_by uuid NOT NULL,
  batch_name text,
  total_files integer NOT NULL,
  status text NOT NULL DEFAULT 'pending'::text,
  processed_count integer DEFAULT 0,
  failed_count integer DEFAULT 0,
  started_at timestamp with time zone,
  completed_at timestamp with time zone,
  errors jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT batch_jobs_pkey PRIMARY KEY (id),
  CONSTRAINT batch_jobs_company_id_fkey FOREIGN KEY (company_id) REFERENCES public.companies(id),
  CONSTRAINT batch_jobs_created_by_fkey FOREIGN KEY (created_by) REFERENCES auth.users(id)
);
CREATE TABLE public.companies (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  name text NOT NULL UNIQUE,
  logo_url text,
  headquarters_address text,
  tax_id text,
  contact_email text NOT NULL,
  phone text,
  timezone text DEFAULT 'UTC'::text,
  awb_stock_threshold integer DEFAULT 50,
  max_team_members integer DEFAULT 20,
  monthly_quota integer DEFAULT 10000,
  metadata jsonb DEFAULT '{}'::jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT companies_pkey PRIMARY KEY (id)
);
CREATE TABLE public.documents (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  company_id uuid NOT NULL,
  created_by uuid NOT NULL,
  filename text NOT NULL,
  file_size_bytes bigint,
  file_url text,
  mime_type text DEFAULT 'application/pdf'::text,
  status USER-DEFINED NOT NULL DEFAULT 'uploaded'::document_status,
  awb_id uuid,
  errors jsonb,
  processing_started_at timestamp with time zone,
  processing_completed_at timestamp with time zone,
  processing_duration_ms integer,
  metadata jsonb DEFAULT '{}'::jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT documents_pkey PRIMARY KEY (id),
  CONSTRAINT documents_company_id_fkey FOREIGN KEY (company_id) REFERENCES public.companies(id),
  CONSTRAINT documents_created_by_fkey FOREIGN KEY (created_by) REFERENCES auth.users(id),
  CONSTRAINT documents_awb_id_fkey FOREIGN KEY (awb_id) REFERENCES public.awbs(id)
);
CREATE TABLE public.extraction_results (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  company_id uuid NOT NULL,
  document_id uuid NOT NULL,
  extracted_data jsonb NOT NULL,
  confidence_scores jsonb DEFAULT '{}'::jsonb,
  average_confidence numeric,
  extraction_model text,
  extraction_version text,
  processing_time_ms integer,
  token_usage jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT extraction_results_pkey PRIMARY KEY (id),
  CONSTRAINT extraction_results_company_id_fkey FOREIGN KEY (company_id) REFERENCES public.companies(id),
  CONSTRAINT extraction_results_document_id_fkey FOREIGN KEY (document_id) REFERENCES public.documents(id)
);
CREATE TABLE public.memberships (
  company_id uuid NOT NULL,
  user_id uuid NOT NULL UNIQUE,
  role USER-DEFINED NOT NULL DEFAULT 'operator'::member_role,
  invited_by uuid,
  invited_at timestamp with time zone,
  accepted_at timestamp with time zone,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT memberships_pkey PRIMARY KEY (company_id, user_id),
  CONSTRAINT memberships_company_id_fkey FOREIGN KEY (company_id) REFERENCES public.companies(id),
  CONSTRAINT memberships_user_id_fkey FOREIGN KEY (user_id) REFERENCES auth.users(id),
  CONSTRAINT memberships_invited_by_fkey FOREIGN KEY (invited_by) REFERENCES auth.users(id)
);
CREATE TABLE public.notifications (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  company_id uuid NOT NULL,
  recipient_id uuid,
  title text NOT NULL,
  message text,
  notification_type text,
  resource_type text,
  resource_id uuid,
  is_read boolean DEFAULT false,
  read_at timestamp with time zone,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT notifications_pkey PRIMARY KEY (id),
  CONSTRAINT notifications_company_id_fkey FOREIGN KEY (company_id) REFERENCES public.companies(id),
  CONSTRAINT notifications_recipient_id_fkey FOREIGN KEY (recipient_id) REFERENCES auth.users(id)
);
CREATE TABLE public.parties (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  company_id uuid NOT NULL,
  name text NOT NULL,
  account_no text,
  address text,
  phone text,
  email text,
  party_type text,
  country_code text,
  metadata jsonb DEFAULT '{}'::jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT parties_pkey PRIMARY KEY (id),
  CONSTRAINT parties_company_id_fkey FOREIGN KEY (company_id) REFERENCES public.companies(id)
);
CREATE TABLE public.profiles (
  id uuid NOT NULL,
  full_name text,
  job_title text,
  phone text,
  avatar_url text,
  city text,
  country text,
  postal_code text,
  timezone text DEFAULT 'UTC'::text,
  is_active boolean DEFAULT true,
  deleted_at timestamp with time zone,
  metadata jsonb DEFAULT '{}'::jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT profiles_pkey PRIMARY KEY (id),
  CONSTRAINT profiles_id_fkey FOREIGN KEY (id) REFERENCES auth.users(id)
);
CREATE TABLE public.usage_logs (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  company_id uuid NOT NULL,
  user_id uuid,
  action text NOT NULL,
  quantity integer DEFAULT 1,
  data_size_bytes bigint,
  metadata jsonb DEFAULT '{}'::jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT usage_logs_pkey PRIMARY KEY (id),
  CONSTRAINT usage_logs_company_id_fkey FOREIGN KEY (company_id) REFERENCES public.companies(id),
  CONSTRAINT usage_logs_user_id_fkey FOREIGN KEY (user_id) REFERENCES auth.users(id)
);