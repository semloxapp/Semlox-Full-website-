You are working on a production Next.js + Supabase SaaS application called SemloX AI Document Engine.

CRITICAL RULES:
- DO NOT modify backend code
- DO NOT modify API routes
- DO NOT modify authentication logic
- DO NOT change Supabase queries
- ONLY modify frontend UI (React + Tailwind)

---

# TASK (VERY IMPORTANT)

Remove the following UI section from the AWB Processing page:

## REMOVE COMPLETELY:
- The breadcrumb/header line:
  "Overview > AWB Processing"
- The file/run metadata line below it:
  "3.PDF - Run mock-run-xxxx"
- Any mock/run identifier text
- Any breadcrumb navigation at top of AWB page

---

# STRICT UI RULES

## 1. AWB PAGE HEADER CLEANUP
Only keep:
- "AWB Processing" title
- main action buttons (New Document, Save Draft, Issue AWB)
- workflow stepper

Everything else above the stepper must be removed.

---

## 2. DO NOT TOUCH:
- backend logic
- API calls
- authentication
- Supabase integration
- database logic
- state management logic

---

## 3. UI SAFETY RULES
- Do not break layout structure
- Do not remove functional components
- Only remove static / redundant UI text elements
- Do not affect PDF viewer
- Do not affect extracted fields logic

---

## 4. DESIGN CONSISTENCY RULES
- Maintain existing SemloX dark/light theme system
- Ensure no layout shift after removing header
- Keep spacing consistent after removal
- Ensure no empty gap remains where header was

---

## 5. FILE SCOPE
Only modify:
- app/dashboard/awb/page.tsx (or AWB page component file)

Do NOT touch any other file.

---

## 6. FINAL EXPECTED RESULT

After changes:
- No breadcrumb ("Overview > AWB Processing")
- No mock/run file text
- Clean minimal AWB header
- UI starts directly from:
  "AWB Processing"
  + buttons
  + stepper

---

END