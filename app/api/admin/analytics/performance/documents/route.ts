import type { NextRequest } from "next/server";
import { NextResponse } from "next/server";
import { requirePlatformAdmin } from "@/lib/admin/server/auth";
import { getAdminPerformanceDocuments } from "@/lib/admin/server/performance-service";
import { parseAdminPerformanceDocumentQuery } from "@/lib/admin/server/validation";
const privateHeaders = { "Cache-Control": "private, no-store, max-age=0" };
export async function GET(request: NextRequest) { const startedAt = Date.now(); const guard = await requirePlatformAdmin(); if (!guard.ok) return NextResponse.json({ ok: false, code: guard.code, message: guard.status === 401 ? "Authentication is required." : "You are not authorized to access this resource." }, { status: guard.status, headers: privateHeaders }); const parsed = parseAdminPerformanceDocumentQuery(request.nextUrl.searchParams); if (!parsed.ok) return NextResponse.json({ ok: false, code: parsed.code, message: parsed.message }, { status: 400, headers: privateHeaders }); try { const data = await getAdminPerformanceDocuments(parsed.value); console.info("[admin-analytics] performance documents", { type: parsed.value.type, page: parsed.value.page, pageSize: parsed.value.pageSize, searchPresent: Boolean(parsed.value.search), returned: data.items.length, durationMs: Date.now() - startedAt }); return NextResponse.json({ ok: true, data }, { headers: privateHeaders }); } catch { console.error("[admin-analytics] performance documents failed", { code: "PERFORMANCE_DOCUMENTS_QUERY_FAILED", durationMs: Date.now() - startedAt }); return NextResponse.json({ ok: false, code: "PERFORMANCE_DOCUMENTS_QUERY_FAILED", message: "Performance documents could not be loaded." }, { status: 500, headers: privateHeaders }); } }
export const runtime = "nodejs"; export const dynamic = "force-dynamic";
